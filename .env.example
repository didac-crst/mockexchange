# =============================================================================
# MockExchange Suite - Environment Configuration
# =============================================================================
# Copy this file to .env and adjust values as needed
# cp .env.example .env

# =============================================================================
# MockExchange Global
# =============================================================================
VERSION=0.1.4

# =============================================================================
# Development Configuration
# =============================================================================
# Test environment flag
TEST_ENV=true
# Development mode
DEBUG=false

# =============================================================================
# Valkey (Redis) Configuration - required by all services
# =============================================================================
# For Docker Compose (default):
VALKEY_HOST=valkey
# For external valkey (uncomment and set your valkey's IP):
# VALKEY_HOST=192.168.178.93
VALKEY_PORT=6379
VALKEY_PASSWORD=SuperSecretPass

# =============================================================================
# MockX Oracle Configuration
# =============================================================================
# Exchange to fetch data from (binance, coinbase, etc.)
EXCHANGE=binance
# Trading pairs to monitor (comma-separated)
SYMBOLS=
# Quote assets for discovery (comma-separated)
QUOTES=USDT
# Fallback quote asset (used if QUOTES is empty)
QUOTE=USDT
# How often to fetch prices (seconds)
INTERVAL_SEC=10
# Auto-discovery mode (true/false)
DISCOVER_QUOTES=true
# Max markets per quote asset (0 = unlimited)
DISCOVER_LIMIT=0

# =============================================================================
# MockX Engine Configuration
# =============================================================================
# API server HOST or IP
ENGINE_HOST=engine
# API server port
ENGINE_PORT=8000
# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
# Commission rate (0.00075 = 0.075%)
COMMISSION=0.00075
# API key for authentication
API_KEY=your-super-secret-key

# =============================================================================
# MockX Periscope Configuration
# =============================================================================
# Dashboard HOST or IP
PERISCOPE_HOST=localhost
# Dashboard port
PERISCOPE_PORT=8501
# Engine API URL (auto-constructed from ENGINE_HOST and ENGINE_PORT):
# For Docker Compose (default):
API_URL=http://${ENGINE_HOST}:${ENGINE_PORT}
# For external engine (uncomment and set ENGINE_HOST):
# ENGINE_HOST=192.168.178.94
# UI base URL for links
UI_URL=http://${PERISCOPE_HOST}:${PERISCOPE_PORT}
# Dashboard title
APP_TITLE=MockExchange
# Logo file (optional)
LOGO_FILE=logo.png
# Auto-refresh interval (seconds)
REFRESH_SECONDS=60
# Quote asset for portfolio valuation
QUOTE_ASSET=USDT
# Fresh window for highlighting (seconds)
FRESH_WINDOW_S=60
# Number of fade-out levels
N_VISUAL_DEGRADATIONS=60
# Slider configuration
SLIDER_MIN=10
SLIDER_MAX=1000
SLIDER_STEP=10
SLIDER_DEFAULT=50
# Timezone for timestamps
LOCAL_TZ=Europe/Berlin