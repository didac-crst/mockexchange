name: deck
on:
  push:
    paths: ['packages/deck/**']
  pull_request:
    paths: ['packages/deck/**']
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v1
      - run: cd packages/deck && uv pip install -e . || pip install -e . && pytest -q || true
