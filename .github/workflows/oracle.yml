name: oracle
on:
  push:
    paths: ['packages/oracle/**']
  pull_request:
    paths: ['packages/oracle/**']
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v1
      - run: cd packages/oracle && uv pip install -e . || pip install -e . && pytest -q || true
