name: backend
on:
  push:
    paths: ['packages/backend/**']
  pull_request:
    paths: ['packages/backend/**']
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v1
      - run: cd packages/backend && uv pip install -e . || pip install -e . && pytest -q || true
